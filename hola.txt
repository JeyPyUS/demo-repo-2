with engine.begin() as conn:
      df.to_sql(
        table_name
        , conn
        , index = False
        , if_exists = if_exists
        , schema = schema_name
        , method = 'multi'
        , chunksize = 10000
        , dtype = dtypes
      )
