import logging.config
import logging

def main(...):
  logging.config.fileConfig(os.path.join(COMMON_PATH, 'Config', cfg_general["log_config_file"]), defaults={
                              'logfilename': os.path.join(COMMON_PATH, f'./Logs/{basename(os.path.dirname(os.path.abspath(__file__)))}.log')})
