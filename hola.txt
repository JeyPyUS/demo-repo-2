SELECT per_exchangerate, COUNT(*)
FROM SAMDataMartPre.dtm_sam_gbl_tb_dd_fac_exchangerate
WHERE cod_source = 0
  AND dat_fact = '20200219'
  AND cod_exchangecurrency ='EUR'
GROUP BY per_exchangerate
HAVING COUNT(*) > 1;
