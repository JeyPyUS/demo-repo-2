def join_dates(tramo, df):
    '''
    Relación de fechas
    '''
    np_fechaobjetivo = df.fecha - pd.DateOffset(months=tramo)
    gap = (df.fecha.values - np_fechaobjetivo).astype(DATA_TYPE).astype("int").values
    idx =  np.arange(len(df)) - gap
    return df.rent.values[idx], gap
